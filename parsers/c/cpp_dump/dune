; cpp_dump links a concrete implementation of the virtual cerb_frontend (any
; will do — it only exercises the preprocessor, which is memory-model agnostic).
(executable
 (name cpp_dump)
 (libraries c_parser mem_concrete))
